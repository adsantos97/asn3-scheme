;;; This is my code
;;; Arizza Santos
;;; CS452 ASN3

(define nil '())

; purpose: find the current item at the front of the data structure
; input: data structure
; output: front of list data structure
 

; purpose: done
; input: element and a data structure
; output: true if the search should stop with failure


; purpose: found
; input: element and current item of the data structure
; output: true if the current item is the element being searched for


; purpose: next
; input: element and a data structure
; output: part of the data structure to be searched next


; purpose: search
; input: data structure, element to search for, 4 functions
; output: element if found, otherwise false
